# AGENTS.md — YAXCHÉ IntentGuard

This repository is a hackathon warrior descended from the YAXCHÉ ecosystem and is governed by the TEC-PATL-S/TZACUA continuity policy.

## Mandatory first read

Before material repository work:

1. Read `TEC_PATL_S_ACCESS.md`.
2. Read `TEC_PATL_S_ACCESS_BOOTSTRAP.json`.
3. Follow `YAXCHE_OPPORTUNITY_LAB.md` to the live central Pizarrón in `Alazthor666/yaxche-ai-core`.
4. Declare motive, operation class and minimum scope.
5. Do not reconstruct current governance from chat memory.

## Project-specific truth boundary

```text
PROJECT_NEW_FOR_HACKATHON = true
PROJECT_START = 2026-08-26
CONCEPTUAL_LINEAGE != COPIED_CODE
CODE_PRESENT != EXECUTED
TEST_WRITTEN != TEST_EXECUTED
DEPLOYMENT_CONFIG != DEPLOYED_SERVICE
```

Any pre-existing source code copied into this repository must be disclosed in `PREEXISTING_WORK_DISCLOSURE.md` with repository, commit and path provenance.

Do not commit credentials or secrets.
